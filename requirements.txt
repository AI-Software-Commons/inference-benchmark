# Runtime dependencies for benchmark_serving.py.
# This tool is an HTTP client. It does not run a model, so it does not need
# torch, xformers, ray, or a server framework.
# Dev tools are in requirements-dev.txt.
# The dataset tool has its own file: datasets/requirements.txt.
# The analysis tool needs only matplotlib: see analysis/README.md.

aiohttp
numpy
requests
transformers >= 4.42.0  # Required for Qwen2. Used for AutoTokenizer only.

# Metrics export
prometheus_client >= 0.21.0

# Google Cloud output upload and auth
google-auth
google-cloud-storage >= 2.18.2
protobuf

# Add sentencepiece if your model uses a slow SentencePiece tokenizer.
